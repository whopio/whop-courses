# Whop Courses

Site for the courses perk on whop

## Getting Started

1. Create a `.env` file: `cp .env.template .env` and fill in the missing values.
2. Run the dev server using: `yarn dev`
3. Finally view the site on [http://localhost:4300](http://localhost:4300) with your browser.
